// src/environments/environment.ts
export const environment = {
  production: false,
  // URL de votre API locale (HTTP pour éviter les problèmes de certificat local)
  apiUrl: 'https://localhost:7147/api/', 
  allowedOrigin: 'http://localhost:4200'
};
